// buildMarquee.js - Continuous scrolling marquee for "WHAT WE BUILD"
// The marquee is now CSS-only animation (keyframes in styles.css).
// This file is kept for backward compatibility but no longer controls the slideshow.
// The slideshow is controlled by buildTabs.js instead.

// No-op — marquee runs via CSS animation
(function() {
  // Nothing needed here; CSS @keyframes build-marquee-scroll handles it.
})();
